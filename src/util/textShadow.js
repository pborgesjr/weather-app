import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  text: {
    textShadowOffset: {
      width: 2,
      height: 2,
    },
    textShadowColor: '#999',
    textShadowRadius: 3,
    elevation: 4,
  },
});

export default styles;
